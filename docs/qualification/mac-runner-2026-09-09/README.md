# Isolated rendered Mac attempt — 2026-09-09

Baseline `b7f1e999d25e2c643a3a58ba5656923aead51952`, plus the narrow Mac XCTest fixture change in this receipt's commit. **Rendered local Mac daily-loop qualification is unavailable. No product assertion or screenshot was produced.**

The existing test `testFocusInterruptionAndReturnJourneyPersists` was selected through XcodeBuildMCP 2.7.0 using stable Xcode 26.6.0, DebugLocal, a task-owned derived-data directory and a 120-second test execution cap. Its intended real UI journey is start work → capture interruption and pause → resume → decline extra capture → end → reopen History with the same note. Test/app compilation passed, resolving pinned PersonalSyncKit 118fc555. Execution then failed because `AnchorMacUITests-Runner` was killed before establishing its XCTest connection. The tool summary reports one failed test; that is a bootstrap/system failure, not a failed product assertion.

The selected fixture now uses its own UUID directory, not merely a unique database filename directly under `/tmp`. This isolates the sibling device-note vault as well as the database, and scoped teardown terminates the fixture app then removes that directory. It also retains screenshots at paused and reopened-note checkpoints when a future run can execute. No screenshots are claimed from this run. The runner failed before this fixture directory was created.

Safety was verified from current source: DebugLocal uses the separate `com.significanthobbies.anchor.local` app and local-only SwiftData; explicit `ANCHOR_STORE_PATH` disables external session synchronization, including the guard before Keychain restoration. The production app, owner store, iCloud, physical devices and iOS simulator were not used. Calorie's headless iOS slot and the other native lane were coordinated before the attempt.

One task-specific crash-report lookup found no matching new runner report. The exact underlying cause is **not established**: do not relabel this as proven Accessibility/TCC denial, signing failure or a product crash. The missing capability is a functioning native XCTest UI runner that can connect on this Mac; alternatively, the existing hosted Mac workflow can provide isolated UI evidence once its separately recorded GitHub billing/spending gate is resolved. No blind retry, CUA startup attempt, permissions change or new unexecutable journey was added.

See [receipt.json](receipt.json) and [the compact build/runner excerpt](runner-attempt.log). Existing unscheduled-habit and usual-week tests remain available, and a Mac copy→edit→play→reopen journey is still uncovered at the rendered level. Before executing those older flat-`/tmp` fixtures, give each selected test the same UUID-directory isolation; do not share their note vault parent. The source/disk-handler evidence in [scheduled-start qualification](../scheduled-start-2026-09-09/README.md) remains valid but is not substituted for rendered UI acceptance.

[#51](https://github.com/Significant-Hobbies/anchor/issues/51) stays open. No install/release or automatic CI occurred; the native workflow is dispatch-only and its known billing-blocked run was not retried. Task build/test products and scratch are removed after retaining this compact receipt; no owner or unrelated test data is cleaned.
